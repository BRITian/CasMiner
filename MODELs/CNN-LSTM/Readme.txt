>>> Here we provide CasMiner(P80) and the homologous model files, as well as the training log file, ROC file and PRC file for each model, and the prediction result files for the generalization ability of each model on four completely independent test datasets: 
1. CasMiner(p80) and the homologous model files [folder: p*]; 
2. Training log files, ROC files, and PRC files [folder: 01_LOG-ROC-PRC]; 
3. Prediction result files for generalization ability [folder: Generalization_results].
